export async function handler(event) {
  const msg = (event.queryStringParameters?.msg || "").trim();

  let resposta = "👋 Olá!\n1️⃣ Atendimento\n2️⃣ Informações\n3️⃣ Sair";

  if (msg === "1") resposta = "📞 Atendimento humano em breve.";
  if (msg === "2") resposta = "ℹ️ Aqui estão as informações principais.";
  if (msg === "3") resposta = "👋 Obrigado! Até logo.";

  return {
    statusCode: 200,
    body: JSON.stringify({ reply: resposta })
  };
}
